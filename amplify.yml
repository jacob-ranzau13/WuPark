version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontEnd
        - rm -f package-lock.json
        - npm install
    build:
      commands:
        - echo "REACT_APP_API_URL=$REACT_APP_API_URL" > .env.production
        - echo "AVAILABILITY_API_KEY=$AVAILABILITY_API_KEY" >> .env.production
        - npm run build
  artifacts:
    baseDirectory: frontEnd/build
    files:
      - '**/*'
  cache:
    paths:
      - frontEnd/node_modules/**/*
  customHeaders:
    - pattern: '**/*'
      headers:
        - key: 'Content-Security-Policy'
          value: "default-src 'self' https://*.amazonaws.com; img-src 'self' data: https://*.tile.openstreetmap.org; style-src 'self' 'unsafe-inline'"
  logging:
    accessLogs:
      - destinationType: CloudWatch
        logGroupName: /aws/amplify/wupark-frontend/access
    applicationLogs:
      - destinationType: CloudWatch
        logGroupName: /aws/amplify/wupark-frontend/application
        logStreamName: ${BUILD_ID}